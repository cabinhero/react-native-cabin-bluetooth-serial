{
  "name": "react-native-cabin-bluetooth-serial",
  "version": "0.0.1",
  "description": "react-native-cabin-bluetooth-serial",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "buffer": "^4.6.0"
  }
}
